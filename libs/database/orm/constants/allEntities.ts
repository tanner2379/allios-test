import { Name } from 'libs/database/entities';

/**
 * All entities in the database, stored in a variable to be shared
 * between the Mikro-ORM configuration file and the Mikro-ORM module.
 */
export const ALL_ENTITIES = [
  //* Name Entities
  Name,
];
